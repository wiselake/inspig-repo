# InsightPig ETL 의존성
# pip install -r requirements.txt

# Oracle Database 연결
# 운영 서버 (Oracle Instant Client 설치됨): cx_Oracle
# 로컬 개발 (Thin 모드): oracledb
cx_Oracle>=8.3.0
oracledb>=2.0.0

# HTTP 요청 (기상청 API, 생산성 API)
requests>=2.28.0

# 스케줄링 (선택)
APScheduler>=3.10.0

# 데이터 처리 (선택)
pandas>=2.0.0

# 환경 변수 관리 (로컬 개발용)
python-dotenv>=1.0.0

# 날짜 계산
python-dateutil>=2.8.0

# REST API 서버
fastapi>=0.104.0
uvicorn>=0.24.0
pydantic>=2.0.0
